const DICT={en:{title:'User Guide',subtitle:'A simple GitHub Gist sync tool.',quickTitle:'Quick start',quickBody:'Create a private Gist or bind an existing one. The extension then reads and writes one shared JSON sync state.',storageTitle:'What is stored',storageBody:'Tabs, tab groups, bookmarks, extension inventory, selected extension settings, sync metadata, tombstones, and conflict records are stored in the Gist.',storageNote:'The sync state is stored as normal JSON. The extension does not encrypt or decrypt the sync payload.',syncTitle:'Sync model',syncBody:'There is no device registry. Every browser reads the same Gist state and merges it with the local base snapshot before writing the next revision.',autoTitle:'Automatic sync',autoBody:'Automatic sync is disabled by default. The default interval is five minutes. You can enable it in Settings and choose another interval.',historyTitle:'History and rollback',historyBody:'GitHub Gist keeps the revision history. The extension does not create separate history files. Rollback writes the selected revision back as a new current revision.',privacyTitle:'Privacy',privacyBody:'New Gists created by the extension are private. GitHub access is authenticated with your Token. Because the sync state is stored as JSON, treat access to the Gist as access to the synced data.',troubleTitle:'Troubleshooting',troubleBody:'If an older Gist reports that it uses the removed encryption format, open it once with v1.5.x and perform a successful sync so the extension can migrate it to the new plaintext format.'},'zh-CN':{title:'用户指南',subtitle:'简洁的 GitHub Gist 同步工具。',quickTitle:'快速开始',quickBody:'创建私有 Gist，或绑定已有 Gist。之后插件只读写一份共享的 JSON 同步状态。',storageTitle:'同步什么',storageBody:'Gist 中保存标签页、标签组、书签、扩展清单、选择的扩展设置、同步元数据、删除标记和冲突记录。',storageNote:'同步状态以普通 JSON 保存。插件不再对同步内容进行加密或解密。',syncTitle:'同步模型',syncBody:'插件不再维护设备注册表。每个浏览器都直接读取同一个 Gist 状态，并结合本地基准快照合并后写入新的 Revision。',autoTitle:'自动同步',autoBody:'自动同步默认关闭，默认间隔为 5 分钟。你可以在设置中开启，并选择其他间隔。',historyTitle:'历史与回滚',historyBody:'GitHub Gist 自己保存 Revision 历史。插件不会再创建额外的历史文件。回滚会把选中的 Revision 写回为新的当前版本。',privacyTitle:'隐私',privacyBody:'插件新建的 Gist 默认是私有的。访问 Gist 需要 GitHub Token。由于同步状态以普通 JSON 保存，应将能够访问该 Gist 视为能够访问同步数据。',troubleTitle:'故障排查',troubleBody:'如果旧版 Gist 提示仍使用已移除的加密格式，请先用 v1.5.x 打开它并成功同步一次，让插件把旧数据迁移为新的明文格式。'}};function detect(){return[navigator.language,...(navigator.languages||[])].some(x=>/^zh(?:[-_]|$)/i.test(x))?'zh-CN':'en';}function apply(){const s=localStorage.getItem('ccsync-guide-language')||'auto',l=s==='auto'?detect():(s==='zh-CN'?'zh-CN':'en'),d=DICT[l]||DICT.en;document.documentElement.lang=l;document.querySelectorAll('[data-guide]').forEach(e=>e.textContent=d[e.dataset.guide]||e.textContent);} function setupChoiceSwitch(root,initialValue,onChange){if(!root)return;const bs=[...root.querySelectorAll('.choice-switch-option')];const set=v=>{root.dataset.active=(v==='en'||v==='dark')?'right':'left';bs.forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.value===v)));};set(initialValue);bs.forEach(b=>b.addEventListener('click',async()=>{const v=b.dataset.value;await onChange(v);set(v);}));}(async()=>{const lang=document.getElementById('language');const s=await chrome.storage.local.get(['language']);if(lang){const lv=s.language&&s.language!=='auto'?s.language:detect();setupChoiceSwitch(lang,lv,async v=>{await chrome.storage.local.set({language:v});localStorage.setItem('ccsync-guide-language',v);apply();});}const theme=document.getElementById('theme');if(theme&&window.CCSyncTheme){await CCSyncTheme.initTheme();const tm=await CCSyncTheme.getTheme();const tv=tm==='dark'||tm==='light'?tm:(document.documentElement.dataset.theme||'light');setupChoiceSwitch(theme,tv,async v=>CCSyncTheme.setTheme(v));}apply();})();