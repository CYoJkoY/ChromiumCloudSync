{
  "name": "chromium-cloud-sync",
  "version": "1.7.9",
  "private": true,
  "type": "module",
  "scripts": {
    "version:sync": "node scripts/sync-package-version.mjs",
    "test": "node scripts/test-sync-core.mjs",
    "validate": "npm run version:sync && npm test && node scripts/validate.mjs",
    "build:zip": "npm run version:sync && node scripts/build.mjs"
  }
}
